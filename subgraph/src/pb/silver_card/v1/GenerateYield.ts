// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.3

import { Writer, Reader } from "as-proto/assembly";

export class GenerateYield {
  static encode(message: GenerateYield, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.txHash);

    writer.uint32(18);
    writer.string(message.user);

    writer.uint32(26);
    writer.string(message.userVault);

    writer.uint32(32);
    writer.uint64(message.amount);

    writer.uint32(40);
    writer.int64(message.timestamp);
  }

  static decode(reader: Reader, length: i32): GenerateYield {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new GenerateYield();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.txHash = reader.string();
          break;

        case 2:
          message.user = reader.string();
          break;

        case 3:
          message.userVault = reader.string();
          break;

        case 4:
          message.amount = reader.uint64();
          break;

        case 5:
          message.timestamp = reader.int64();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  txHash: string;
  user: string;
  userVault: string;
  amount: u64;
  timestamp: i64;

  constructor(
    txHash: string = "",
    user: string = "",
    userVault: string = "",
    amount: u64 = 0,
    timestamp: i64 = 0
  ) {
    this.txHash = txHash;
    this.user = user;
    this.userVault = userVault;
    this.amount = amount;
    this.timestamp = timestamp;
  }
}
